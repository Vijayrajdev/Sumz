import { logo } from "../assets";
const Hero = () => {
  return (
    <div>
      <header className="w-full flex justify-center items-center flex-col"></header>
    </div>
  );
};

export default Hero;
